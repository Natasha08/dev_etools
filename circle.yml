machine:
  node:
    version: 4.4.7

dependencies:
  pre:
    - npm install -g karma
    - npm install -g karma-cli

install: 
 - npm install -g codecov
 
script: 
 - istanbul cover node_modules\jasmine\bin\jasmine.js  
